.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# MM1 firmware — GitHub Releases + Actions artifacts only (never commit)
/MM1-BLACK-denky32-*.bin
/MM1-BLACK-denky32-*.bin.sha256
dist/
docs/flasher/bins/

# Cursor IDE — pasta local (memórias, regras experimentais); não versionar
.cursor/
.DS_Store
**/__pycache__/
/.dummy/

# Sequência 360° experimental substituída pelas dez fotografias finais
/docs/site/assets/rotation-360/

# Paper IEEE LARS 2026 — fontes, datasets e bags ficam fora do repo de firmware
paper/
texput.log

# Backup gerado pelo build do ESP32_Display_Panel ao reescrever o manifesto IDF
src/idf_component.yml.orig

# pioarduino HybridCompile (custom_sdkconfig) — generated locally
sdkconfig
sdkconfig.old
sdkconfig.defaults
sdkconfig.defaults.orig
dependencies.lock
managed_components/
