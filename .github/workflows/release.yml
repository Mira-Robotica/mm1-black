name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio-p4-${{ hashFiles('platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-p4-

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Resolve version from tag
        id: ver
        run: |
          TAG="${GITHUB_REF_NAME}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Build firmware (P4)
        env:
          VERSION: ${{ steps.ver.outputs.tag }}
        run: pio run -e mm1_p4

      - name: Package binary (Release asset only — never committed)
        run: |
          TAG="${{ steps.ver.outputs.tag }}"
          mkdir -p dist
          cp .pio/build/mm1_p4/firmware.bin "dist/MM1-BLACK-mm1_p4-${TAG}.bin"
          sha256sum dist/MM1-BLACK-mm1_p4-${TAG}.bin | tee dist/SHA256SUMS

      - name: Resolve release notes (English)
        id: notes
        run: |
          TAG="${{ steps.ver.outputs.tag }}"
          FILE="docs/releases/${TAG}.md"
          if [ -f "$FILE" ]; then
            echo "path=$FILE" >> "$GITHUB_OUTPUT"
            echo "Using release notes: $FILE"
          else
            echo "No docs/releases/${TAG}.md — falling back to auto-generated notes"
          fi

      - name: Publish GitHub Release (English notes)
        if: steps.notes.outputs.path != ''
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/MM1-BLACK-mm1_p4-${{ steps.ver.outputs.tag }}.bin
            dist/SHA256SUMS
          body_path: ${{ steps.notes.outputs.path }}

      - name: Publish GitHub Release (auto notes)
        if: steps.notes.outputs.path == ''
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/MM1-BLACK-mm1_p4-${{ steps.ver.outputs.tag }}.bin
            dist/SHA256SUMS
          generate_release_notes: true
