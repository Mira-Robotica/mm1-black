name: CI

on:
  push:
    branches: [main, dev, "feat/**"]
  pull_request:
    branches: [main, dev]

jobs:
  build-p4:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio-p4-${{ hashFiles('platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-p4-

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      - name: Build mm1_p4
        run: pio run -e mm1_p4

      - name: Package firmware artifact
        run: |
          mkdir -p dist
          SHA="${GITHUB_SHA::7}"
          OUT="dist/MM1-BLACK-mm1_p4-${SHA}.bin"
          cp .pio/build/mm1_p4/firmware.bin "$OUT"
          sha256sum "$OUT" > "${OUT}.sha256"

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: MM1-BLACK-mm1_p4-${{ github.sha }}
          path: dist/
          if-no-files-found: error
          retention-days: 90
